export default function RoleSelect() {
  return (
    <select className="rounded-full border border-[var(--stroke)] bg-white px-3 py-2 text-sm">
      <option>Viewer</option>
      <option>Editor</option>
    </select>
  );
}
